version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - APP_ENV=development
    volumes:
      - ./app:/app/app
    restart: unless-stopped
